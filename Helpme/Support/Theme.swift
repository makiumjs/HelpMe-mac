import SwiftUI

/// Colori dell'identità dell'app, definiti una volta sola.
/// Prima il verde istituzionale era riscritto a mano in 24 punti diversi.
public extension Color {
    /// Verde dell'istituto, lo stesso usato nell'intestazione dei .docx.
    static let institutional = Color(hex: 0x1E4620)
    static let institutionalMid = Color(hex: 0x2E7D32)
}
